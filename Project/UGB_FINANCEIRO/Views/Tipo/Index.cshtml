@model IEnumerable<UGB_FINANCEIRO.Models.Tipo>

@{
    ViewBag.Title = "Index";
}

<div class="crud">
    <div class="titulo">
        MÓDULO FINANCEIRO
    </div>
    <div class="content">
        <div class="display-data">
            Data Hoje: @DateTime.Now.Date.ToString("dd/MM/yyyy")
        </div>
        <div class="btn-cadastrar">
            @Html.ActionLink("Cadastrar Mensalidade", "Create", null,
                            new { @class = "btn btn-primary" })
        </div>
        <table class="table">
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Tipo1)
                </th>
                <th></th>
            </tr>

            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Tipo1)
                    </td>
                    <td>
                        @Html.ActionLink("Alterar", "Edit", null, new { id = item.idTipo }, new { @class = "btn-acao" })
                        @Html.ActionLink("Deletar", "Delete", null, new { id = item.idTipo }, new { @class = "btn-acao" })
                    </td>
                </tr>
            }

        </table>
    </div>
</div>